// Rock Paper Scissors Game

const game = () => {
var options = ["R", "P", "S"] 
var message = "eat butt"

if (window.confirm("Do you want to play again?")) {
    window.open("exit.html", "Thanks for playing!");
}

if (window.prompt(options)) {

}


};
window.alert(game);